# Enforcement ON (the default; set explicitly so the test is self-contained).
# Sanity: API node 1600 is currently connected.
# Inject a Tier A violation (vtype=0 = UNEXPECTED_API_STATE) against the
# connected API node 1600, with enforcement ON.
# Tier A + enforcement => node 1600 is disconnected (leaves processes).
# (API node 1600 disconnected by enforcement: OK)
# Cleanup: wait for the pool to reconnect 1600 so the cluster is healthy
# again for any following test.
