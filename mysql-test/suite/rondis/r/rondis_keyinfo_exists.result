#
# Set up: a string key, a populated hash, a hash that's about
# to be emptied, and an absent name.
#
OK
2
1
1
#
# Existing string key.
#
1
#
# Existing hash key.
#
1
#
# Fully-HDEL'd hash. 1.10c.8/D drops the hset_keys row in the
# same trans, so EXISTS reports 0.
#
SELECT COUNT(*) AS hash_empty_rows FROM redis_0.hset_keys
WHERE redis_key = 'hash_empty';
hash_empty_rows
0
0
#
# Missing key.
#
0
#
# Mixed batch - 1 string + 1 hash + 1 missing + 1 emptied = 2.
#
2
#
# Duplicates count each occurrence per Redis canonical.
#
4
#
# Case-sensitivity: STR_A != str_a.
#
0
