#
# Set up: a string, a populated hash, a hash about to be emptied.
#
OK
1
1
1
#
# Existing string key.
#
string
#
# Existing hash.
#
hash
#
# Fully-HDEL'd hash. Row exists with field_count=0; TYPE returns
# none (matching EXISTS = 0).
#
SELECT redis_key, field_count FROM redis_0.hset_keys
WHERE redis_key = 'hash_empty';
redis_key	field_count
hash_empty	0
none
#
# Missing key.
#
none
#
# Unified namespace: HSET on an existing string is rejected, so
# TYPE still reports "string".
#
OK
ERR WRONGTYPE Operation against a key holding the wrong kind of value

string
#
# After deleting the string, TYPE on the same name reports
# "none"; no hash row was created by the rejected HSET.
#
1
none
