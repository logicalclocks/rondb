--source include/have_ndb.inc

--echo Running testJoinAggScanScan
--exec $NDB_PUSH_AGG_DIR/testJoinAggScanScan -c "$NDB_CONNECTSTRING" -m $MASTER_MYPORT > /dev/null 2>&1
--echo PASSED

# Clean up database created by test program
DROP DATABASE IF EXISTS test_db;
