# Findings — envelope-level random generator (E7)

Format as `smoke.md`; `#` continues the shared numbering.  Family:
envelope (engine finding on a shape beyond Hopsworks' current emitter) /
hazard (a discovery-log HANG/CRASH translated to this schema, run with
`--include-hazards`) / framework.  Each engine finding is cross-referenced
to the `_discovery_log.md` conventions (D-ids) where the shape matches.
One row per signature (`CASE … <production>|<constructs>`).

| # | Family | Signature | Symptom | Repro (minimal) | Disposition |
|---|--------|-----------|---------|-----------------|-------------|
| F15 | envelope (engine) | `cte-per-fg`: an aggregating main that LEFT JOINs a CTE_LOOKUP whose key misses (isolated single-threaded to env-v1-1-9's predecessor, env-v1-1-8) | **DATA NODE CRASH** — both `ndbmtd` abort with error 2343 `Internal program error (failed ndbassert)`, `DBSPJ (Line: 15201) DbspjMain.cpp Check false failed`; stack `Dbspj::appendFromParent` ← `Dbspj::expand` ← `Dbspj::sendJoinAggNullRow` ← `Dbspj::execCTE_LOOKUP_REF` (the null-row path of a LEFT JOIN whose right side is a CTE_LOOKUP aggregation leaf). After the crash every case reports MySQL error 4009 `No data node(s) available`. | `WITH tx AS (SELECT customer_id AS k, COUNT(*) AS n, SUM(amount) AS s FROM transactions_1 WHERE customer_id IN (1012, 644, …, 1829) GROUP BY customer_id) SELECT c.customer_id, MAX(tx.n), SUM(tx.s) FROM customers_1 AS c LEFT JOIN tx ON tx.k = c.customer_id GROUP BY c.customer_id;` (sf 0.01; the IN list includes ids > E=1000, so the LEFT join has misses; `fuzz/hazards.go` F15) | OPEN — engine tree (RONDB-733 pushdown join aggregation): `sendJoinAggNullRow` / `appendFromParent` in `DbspjMain.cpp:15201`. Crash artifacts in `debug_build/mysql-test/var/mysql_cluster.1/ndbd.*/ndb_*_{error,trace}.log*`. Related discovery-log crash class D18/D23. |

| F18 | envelope (engine) | `probe\|probe,cte-partial-key` (4 of 1000, seed 2) | **WRONG RESULT**: a partial-key CTE lookup (the join binds fewer than all of the CTE's virtual-key columns) that used to reject cleanly (`Partial CTE lookup key not supported.`) now RUNS and returns the wrong row count (MySQL 400, RonSQL 229). A rejection turned into silently wrong data. | `WITH \`x\` AS (SELECT \`account_id\` AS \`k1\`, \`currency\` AS \`k2\`, COUNT(*) AS \`n\` FROM \`balance_hist_1\` GROUP BY \`account_id\`, \`currency\`) SELECT \`b\`.\`account_id\`, SUM(\`x\`.\`n\`) AS \`n\` FROM \`balances_1\` AS \`b\` JOIN \`x\` ON \`x\`.\`k1\` = \`b\`.\`account_id\` GROUP BY \`b\`.\`account_id\`;` | OPEN — engine tree: the partial-key CTE_LOOKUP guard no longer fires and the lookup binds only part of the virtual key, over-/under-counting rows. Previously a clean reject (expectation table); reclassified `KnownWrong` (F18). |
| F19 | envelope (engine) | `probe\|probe,cte-scan-outer-child` (3 of 1000, seed 2) | **WRONG RESULT**: a CTE (several groups) on the right of a LEFT JOIN — CTE_SCAN as an outer-join child — used to reject (`CTE_SCAN as outer-join child is not supported by NDB.`); it now RUNS and its output diverges from MySQL (output names / columns differ). | `WITH \`t\` AS (SELECT \`merchant_id\` AS \`k\`, COUNT(*) AS \`n\` FROM \`transactions_1\` WHERE \`customer_id\` = 678 GROUP BY \`merchant_id\`) SELECT \`m\`.\`mcc\`, SUM(\`t\`.\`n\`) AS \`n\` FROM \`merchants_1\` AS \`m\` LEFT JOIN \`t\` ON \`t\`.\`k\` = \`m\`.\`merchant_id\` WHERE \`m\`.\`merchant_id\` = 1 GROUP BY \`m\`.\`mcc\`;` | OPEN — engine tree: CTE_SCAN outer-join child no longer rejected but not correctly executed. Reclassified `KnownWrong` (F19). |
| inner-below-left | envelope (resolved) | `probe\|probe,inner-below-left` (6 of 1000, seed 2) | An INNER JOIN nested under a LEFT JOIN — previously rejected (`INNER JOIN below LEFT JOIN not supported.`) — now RUNS and agrees with MySQL. | `WITH \`b\` AS (…) SELECT \`j2\`.\`region_name\`, \`j3\`.\`country_name\` FROM \`b\` LEFT JOIN \`regions_1\` AS \`j2\` ON … JOIN \`countries_1\` AS \`j3\` ON …;` | RESOLVED (engine): the limit is lifted and the result is correct. Expectation-table row removed; the shape now passes as a regular case. |
| F16 | envelope (engine) | `probe\|probe,avg-string` (2 of 200, seed 1) | AVG over a VARCHAR column is rejected — correct — but with the generic `Failed writing aggregation program. Please report a bug.` instead of the specific guard `AVG over string columns is not supported.`, which the source has and which the AVG-over-temporal path uses. The string-column AVG guard is bypassed and the failure falls through to aggregation-program writing. | `SELECT AVG(\`category\`) AS \`a\` FROM \`transactions_1\` WHERE \`customer_id\` = 764;` | OPEN — engine tree: message quality / guard placement in `RonSQLPreparer.cpp` (the `AVG over string columns` check does not cover this path). Functionally a clean rejection; the fuzzer treats it as CLEAN-REJECT via the expectation table. |
| F17 | envelope (engine) | `having-order\|…,having` with ORDER BY on an aggregate alias + LIMIT (seed 1) | HAVING combined with ORDER BY (on an aggregate alias) and LIMIT drives an internal error `Got record with fewer aggregates than expected. Please report a bug.` — a worse outcome than plain HAVING, which rejects with `Could not find column`. The ORDER-BY / LIMIT drain over a HAVING-filtered aggregate loses aggregate records. | `SELECT \`customer_id\`, COUNT(*) AS \`n\`, SUM(\`amount\`) AS \`amount_sum\` FROM \`transactions_1\` WHERE \`customer_id\` IN (…) GROUP BY \`customer_id\` HAVING \`n\` > 2 ORDER BY \`n\` DESC, \`customer_id\` ASC LIMIT 9;` | OPEN — engine tree: internal error in the ORDER BY/LIMIT drain of a HAVING-filtered aggregate (`RonSQLPreparer.cpp` / ResultPrinter). HAVING is unsupported, so the clean fix is to reject HAVING uniformly before the ORDER BY path. Folded under the `having` expectation row (tagged-only, alt-pattern) accepting both the plain-HAVING `Could not find column` and this internal error. |
| HAVING | envelope (expected) | `having-order\|…,having…` (11 of 200, seed 1) | HAVING referencing an aggregate output alias (`HAVING \`n\` > 2`) rejects with `Could not find column (column names are case sensitive).`: RonSQL resolves HAVING against base columns, not SELECT aliases. Consistent with the corrected-envelope doc listing HAVING as unsupported. | `SELECT \`customer_id\`, COUNT(*) AS \`n\` FROM \`transactions_1\` WHERE \`customer_id\` IN (…) GROUP BY \`customer_id\` HAVING \`n\` > 1;` | EXPECTED — HAVING is outside the RonSQL envelope; expectation-table row `having` (tagged-only, so the generic message is matched only for HAVING cases). ORDER BY on an aggregate alias + LIMIT (without HAVING) is supported and passes. |

Known outcomes that are not findings: `CLEAN-REJECT` on the probe
production and the CTE-form collect (expectation table), `KNOWN-WRONG`
on string-keyed snowflakes (F14), `KNOWN-ERROR` on temporal MIN/MAX
(F9), `KNOWN-HAZARD` on the discovery-log hazards, `PASS(hazard)` /
`PASS(was-expected-reject)` when the engine has moved.
