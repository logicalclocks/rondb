--source include/have_ndb.inc

call mtr.add_suppression("Schema dist coordinator detected timeout");
call mtr.add_suppression("Participant timeout");
--let $suppress_ronsql_cli=yes
--let $strict_diff=yes

--disable_query_log
--disable_warnings

CREATE TABLE j_customer (
  id INT NOT NULL,
  `name` VARCHAR(20) NOT NULL,
  PRIMARY KEY USING HASH (id)
) ENGINE=NDB;

CREATE TABLE j_orders (
  id INT NOT NULL,
  cust_id INT NOT NULL,
  status INT NOT NULL,
  PRIMARY KEY USING HASH (id),
  INDEX idx_orders_cust (cust_id)
) ENGINE=NDB;

CREATE TABLE j_lineitem (
  id INT NOT NULL,
  order_id INT NOT NULL,
  qty INT NOT NULL,
  price INT NOT NULL,
  PRIMARY KEY USING HASH (id),
  INDEX idx_li_order (order_id)
) ENGINE=NDB;

INSERT INTO j_customer VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');

INSERT INTO j_orders VALUES
  (10, 1, 1), (11, 1, 2),
  (20, 2, 1), (21, 2, 2),
  (30, 3, 1), (31, 3, 2);

INSERT INTO j_lineitem VALUES
  (100, 10, 5, 50), (101, 10, 3, 30),
  (102, 11, 7, 70), (103, 11, 2, 20), (104, 11, 1, 10),
  (105, 20, 10, 100), (106, 20, 4, 40),
  (107, 21, 6, 60),
  (108, 30, 8, 80), (109, 30, 9, 90), (110, 30, 1, 10),
  (111, 31, 3, 30);

--enable_warnings
--enable_query_log

--let $MYSQL= $MYSQL --init-command-add='SET sql_mode=""'

DELIMITER |;

--echo === Run 1 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 2 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 3 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 4 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 5 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 6 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 7 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 8 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 9 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

--echo === Run 10 ===
let $QUERY=
SELECT o.id AS order_id,
  (SELECT SUM(l.price) FROM j_lineitem AS l WHERE l.order_id = o.id AND l.qty > 3) AS big_total
FROM j_customer AS c
JOIN j_orders AS o ON o.cust_id = c.id;|
--source suite/ronsql/include/ronsql_compare.inc

DELIMITER ;|

--echo === Cleanup ===
--disable_query_log
DROP TABLE IF EXISTS j_lineitem;
DROP TABLE IF EXISTS j_orders;
DROP TABLE IF EXISTS j_customer;
--enable_query_log
