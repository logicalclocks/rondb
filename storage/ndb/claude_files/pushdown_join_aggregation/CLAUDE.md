# DBLQH Block

DBLQH (DataBase Local Query Handler) handles scan and key operations on data node LDM threads.

## Pushdown Join Aggregation (RONDB-733)

DBLQH supports aggregation for pushdown join queries, allowing aggregation to be
pushed down to data nodes so intermediate results don't round-trip to the API.

### Documentation (in this directory)
- `local_database_research.md` — Local database (DBLQH/DBTUP) research
- `local_database_implementation.md` — Local database implementation details
- `coordinator.md` — DBSPJ coordinator design
- `coordinator_research.md` — DBSPJ coordinator research
- `coordinator_implementation.md` — DBSPJ coordinator implementation details
- `trace_file_analysis.md` — NDB trace/crash log analysis guide
- `agg_interpreter_unification_plan.md` — Analysis + 3-step plan to deduplicate `AggInterpreter` and `JoinAggInterpreter` and give `AggInterpreter` the better `JoinAggInterpreter` memory model.  **Steps 1, 2, 3a, 3b, and 4 complete** (1.1 + base-class foundation + 1.2 + 1.3 + 1.4 + 2a + 2b + 2c + 3a-A + 3a-B + 3b + 4a + 4b + 4c + 4d).  **Step 1** (`AggInterpreterBase.{hpp,cpp}`: 14 shared numeric/type kernels as `protected static`, `validateEmbeddedProgram` (strict form), `OptimizeProgram` (public non-virtual), string MIN/MAX helpers `minMaxString` / `stringPayloadSize` / `encodeStringPayload` / `freeGroupStringSlots` / `hasStringSlots` / `string_results`, `executeStandardOpcode` (28 opcode arms), `m_prog` / `m_agg_prog_start_pos` / `m_n_agg_results` / `m_agg_results` / `m_registers[]` / `m_register_string_data[]` / `m_string_results` / `m_current_thread_id` / `m_decimal` / `m_decimal_buf[]` lifted; `DECIMAL_BUFF_LENGTH` macro replaced by `AGG_DECIMAL_BUFF_LENGTH` in-class constexpr; per-class kOpLoadCol + kOpEmbeddedInterp stay).  **Step 2** lifted the chunk allocator (`initChunkAllocator` / `allocNewChunk` / `allocGroupData` / `freeGroupData` / `bookMoreMemory` / `freeAllChunks` / `GROUP_LINK_OVERHEAD`) + GB-type metadata (`m_gb_types` / `m_gb_types_inited` / `m_xfrm_buf` / `m_xfrm_buf_len`) + `m_n_gb_cols` / `m_gb_cols` / `m_gb_map` / `m_n_groups` to the base; parametrized `initGBTypes(linked_attr_data, linked_attr_len)` lifted (linked-attr branches dead-code on normal-scan); both interpreters use the same `JoinGBHashTable` (1024 buckets) per maintainer direction; AggInterpreter's `std::map` + `m_mem_buf` bump pool + `MemAlloc` + `initGBCmpCtx` + `m_gb_cmp_ctx` removed and replaced with hash-table + chunk-allocator group prologue, `GBHashTable::Iterator`-based streaming drain, `release_string_results` and `Print` adapted.  Public NdbAggregator API-side `GBHashEntry` / `GBHashEntryCmp` / `GBCmpContext` / `GBColMeta` retained (NdbAggregator still uses `std::map`).  **Step 3a** lifts the remaining scratch / static / `release_string_results` bits (3a-A) and unifies the allocation model (3a-B): both interpreters now carve a right-sized `m_buf_block` from `RG_QUERY_MEMORY` via the shared `initBufBlock` helper; `~AggInterpreterBase()` is the single teardown site (calls `release_string_results` → `freeAllChunks` → frees `m_xfrm_buf` → frees `m_buf_block`); AggInterpreter loses all inline arrays so its object header shrinks from ~30 KB to ~few hundred bytes; `static_assert(sizeof(...) <= MEM_CHUNK_SIZE)` removed from both subclasses; query-sized footprint is ~8 KB for `SELECT COUNT(*)` (vs 30 KB pre-3a).  Maintainer chose option (a) — AggInterpreter stays as a thin normal-scan subclass; the architectural split between streaming-drain (Agg) and persist+merge+emit (JoinAgg) is real and load-bearing.  **Step 3b** is the targeted dedup follow-up: deletes dead `Print()` + `getResultData()`, lifts trivial inline accessors (`gb_map` / `val_len` / `n_gb_cols` / `n_agg_results` / `agg_results` / `processed_rows`) to the base, and factors `scanAndValidateEmbeddedPrograms` as a shared Init-tail helper.  Per maintainer direction, option (a) is the final shape: AggInterpreter stays as a thin normal-scan subclass; the architectural split between streaming-drain (Agg) and persist+merge+emit + outer-join/multi-leaf/CTE/cross-node (JoinAgg) is real and load-bearing and not further collapsed.  ~2400 dup lines removed total across Steps 1 + 2 + 3; build + agg tests + RonSQL/CTE MTR green.  ~1,900-2,100 duplicated lines (numeric kernels, opcode loop, string MIN/MAX, validator, wire format) identified.  **Step 1** extracts the shared compute engine behind a base class (zero behavior change).  **Step 2** replaces AggInterpreter's `std::map`+inline-bump allocator with the shared `GBHashTable`(256-bucket)+chunk allocator while *keeping* AggInterpreter's existing streaming per-batch `Signal` drain (`PrepareAggResIfNeeded`/`NumOfResRecords`) — the drain is the normal-scan-result-to-API path, just re-pointed at the hash table (same emit→erase→free as `evictOneGroup`); CTE/merge/redistribute/linked-attr machinery stays JoinAgg-only.  **Step 3** (deferred) decides whether to fully fold `AggInterpreter` away.  Footprint tension handled via the pre-positioned 256-bucket `AggGBHashTable` alias + drain-every-batch.  Single creation site = `scanCopyAttrinfo`.  **Step 4** (shipped) is the post-3b sweep: **4a** `86627c3ef31` moves `m_attr_read_buf` to a per-LDM-thread 72 KB scratch on Dbtup (`getAggAttrReadBuf`), bound per-`ProcessRec` call; **4b** `a9aed46c175` (Cand-A, Init prologue/epilogue) + `6ef2ae2bd17` (Cand-B, `kOpLoadCol` → `loadColumnTypedFromBuf`); **4c** bounded teardown `beginTeardown` / `tearDownChunk(N)` via CONTINUEB (`Dblqh::continueAggInterpTeardown` + `DblqhProxy::continueJoinAggTeardown`), `~AggInterpreterBase()`/`~ScanRecord()` now `ndbrequire` already-drained; **4d** `934ef2993f3` CTE join-agg routing + self-feed hardening — fixes a multi-node `ronsql_cte_partial_key` SIGSEGV where `CTE_LOOKUP_ROUTE_FLAG` let a remote owner misread the node-local target `joinAggStateKey` as its own source CTE state and fed the source aggregator (local GROUP BY cols) → poisoned `tablePtrP` deref; DBSPJ `cte_lookup_send` now normalizes the virtual GROUP BY AttributeHeaders to column positions, hashes via the local CTE interp `hashGroupKey`, and routes CTE_LOOKUP directly to the owner with that node's source+target agg keys (`CTE_LOOKUP_ROUTE_FLAG` retired, scalar lookups stay local); self-feed guard `ZCTE_AGG_FEED_SELF_REFERENCE` (1269) rejects `targetState == sourceState`; linked-only guard sets `tablePtrP = nullptr` at all 4 CTE-feed `KeyReqStruct` sites and aborts `initGBTypes` / GB-col read / `kOpLoadCol` with `ZAGG_OTHER_ERROR` on any normal-column read; `jamBuffer()` threaded through `processRecWithLinkedAttrs` / `processNullExtendedRow` / `ProcessRec` / `initGBTypes`. See Step 4 in the plan doc
- `next_steps.md` — Remaining work: MySQL handler integration, secondary features
  (references RonSQL subquery plan in `storage/ndb/src/ronsql/ronsql_join_phase7.md`)
- `cte_mutex_free_build_plan.md` — **Step 1 SHIPPED (2026-07-04, mostly 2-3x speedup on CTE benchmarks)**: MUTEX_FREE CTE materialization build with thread-merge before redistribute.  CTE agg states were hardcoded `STRATEGY_MUTEX_BASED` (DbtcMain.cpp:30134) so every body-scan row serialized through one shared `JoinAggInterpreter` + `std::mutex` per node (root cause of `cte_tpch_q15`/`q22` being ~4x slower than MySQL baselines; q22 additionally pays per-row CTE_LOOKUP probes — separate follow-up).  Audit found ALL machinery already existed strategy-aware (per-thread CTE-configured setup in DblqhProxy, `continueJoinAggMerge` CONTINUEB-sliced `mergeFrom` into `[0]` ahead of the CTE redistribute tail, scalar/string/group merge support, result-interpreter selection in probe/walk/drain/inbound-merge paths, DBSPJ hash metadata, teardown) — the functional change was flipping the DBTC CTE setup to `STRATEGY_MUTEX_FREE`; `STRATEGY_MUTEX_BASED` survives only as the DblqhProxy decode fallback for old-version DBTCs.  Doc carries the concurrency argument (owner-instance pinning, commutative early inbound merges, feeds-done-before-COMPLETE barrier) and the open watch items: memory-budget review on lg_* data (transient duplicate groups ≤ threads×, per-thread budget split), eviction error-insert gating, redist-queue growth during the longer FINALIZING window
- `ronsql_cli_benchmarks.md` — named analytics queries in `rondb-cli` over the CLI's `tpch` database (`.load_tpch`): online (`fs_*`) + offline (`offline_fs_*`) Feature-Store-style CTE queries, TPC-H CTE rewrites (`tpch_q2/q11/q13/q15/q22`, `.bench_sql` name `cte_tpch_q*`), and official TPC-H formulations (MySQL-only `tpch_q*`); run via `.bench_ronsql <name>` (RonSQL REST) or `.bench_sql <name>` (MySQL comparative baseline); inspect via `.list_query_ronsql`/`.list_query_sql`, `.query_ronsql`/`.query_sql <name>`, `.explain_ronsql`/`.explain_sql <name>` (RonSQL explainMode=FORCE / MySQL EXPLAIN + SHOW WARNINGS pushdown notes); registry in `tools/rondb-cli/internal/shell/ronsql_bench.go`, inspection in `query_inspect.go`; per-query envelope deviations, warmup validation, µs latency percentiles, and the phase-timing follow-up plan (item 1 now SHIPPED as the `x-ronsql-phases` header — see `test_benchmark_extension_plan.md`)
- `test_benchmark_extension_plan.md` — **SHIPPED (recorded ×5 topologies) + bug-fix phase COMPLETE (2026-08-20)**: test + benchmark extension for pushdown join agg and CTEs.  Every §5b finding is fixed and re-enabled as live regression cases: EXPLAIN `SUM(CASE)` abort + 2 ORDER BY explain misprints, scalar-CTE duplicate feed, DECIMAL-subquery RDRS crash, `NdbInterpretedCode::finalise()` non-idempotency (NOT-heavy join-root filters), the big-07 hang (outer/anti CTE join + second CTE join — `CTE_LOOKUP_OUTER_CHAIN_FLAG` row-shaped protocol + completion-sweep exclusion, now big-07a/b/c), and the big-06 8-node RDRS crash (mixed-signedness CASE-arm partial merging — shared `aggMergeNumericSlot` helpers in `NdbAggregationCommon.hpp` + kernel `Max()`/`Min()` arm fixes, now big-06 + big-06b).  Open follow-ups live in §7 (TODO 7.1 CASE-as-WHERE-predicate, 7.2 SQL comment lexing, 7.3 end-to-end merge-overflow propagation) and §8 (candidate future test phases, P0 done).  MTR: data-type width×signedness×boundary family (`body_dtwide.inc`, dtw-01..20 ×5 topologies), larger combined-query family (`body_bigquery.inc`, big-01..12 ×5), parser coverage for the CTE/join/hint/FRAGS_PER_WORKER grammar (`ronsql_parser_cte`), subquery×CTE + subquery result types (`ronsql_cte_subquery`), MySQL-handler pushed-agg datatype sweep (`ndb_join_pushdown_agg_types`), phase-header format pin (`ronsql_phase_stats`).  Benchmarks: `fs_floor`/`fs_minmax`/`fs_dnf`/`offline_fs_wide`/`offline_fs_chain` registry entries + per-phase avg/p95/p99/max breakdown in `.bench_ronsql`.  Phase stats: `RonSQLPhaseStats` sink on `RonSQLExecParams`, STAT_TS/STAT_SET capture at the PERF_TS boundaries (parse/analyze/load/plan/compile + subquery/ndbprep/send/firstbatch/drain/print + rows/attempts), gated by `RONSQL_PHASE_STATS` (default on, RonSQLPerf.hpp), emitted by RDRS as the `x-ronsql-phases` response header (last-attempt-wins), parsed by `ronsql_bench.go`.  Recording-pass findings + future-extension TODOs (§7, deliberately NOT in these phases): CASE as a general WHERE predicate (grammar has CASE only as an aggregate-argument expression; constant-armed DNF desugar sketched) and SQL comment lexing (RonSQLLexer.l skips only whitespace — every comment form is a syntax error)
- `ndbapi_integration_plan.md` — NDB API integration plan (Steps 1-10, all complete)
- `ndbapi_integration_implementation.md` — NDB API integration implementation details
- `mysql_join_agg.md` — MySQL handler integration architecture (Phase 9)
- `mysql_handler_implementation.md` — MySQL handler implementation plan (Phases 1-12)
- `chained_outer_join_plan.md` — Chained outer join aggregation fix plan (Phases 1-6)
- `star_schema_plan.md` — Star schema fan-out aggregation design (RONDB-1044)
- `star_schema_implementation.md` — Star schema implementation plan (Steps 1-12)
- `cte_filter_plan.md` — CTE filter + 3rd interpreter (jump-table) overview + phase index
- `cte_filter_phase_a.md` — Phase A: CTE_LOOKUP_REQ filter support
- `cte_filter_phase_b.md` — Phase B: CTE_SCAN_REQ (root) filter support
- `cte_filter_phase_c.md` — Phase C: aggregation interpreter reuses the jump-table interpreter
- `cte_filter_phase_d.md` — Phase D: inline-type opcode for synthesized columns (RonSQL)
- `cte_filter_phase_d2.md` — Phase D2: MIN/MAX virt-type widening for inline filter (RonSQL)
- `cte_filter_minmax_strings_plan.md` — Phase I.6: MIN/MAX over CHAR / VARCHAR / DECIMAL.  F.1 shipped (`d812019e9a4`): DECIMAL widening to BIGINT/Bigunsigned/DOUBLE in CTE virt-table types, mirroring the kernel's `AggInterpreter::AlignedType`; new `out_scale` return on `resolve_chained_column_type`
- `cte_filter_phase_i6_varchar.md` — Phase I.6 F.2 + F.3 (shipped): kernel string MIN/MAX execution via per-(group, slot) val_ptr state, AGG_CHAR_RESULT wire format (marker `0xFF02`), NdbAggregator API parse + `Result::data_str` accessor, RonSQL CTE virt-table type passthrough and filter accept-list extension.  Historical follow-ups (linked-attr strings, CTE delivery substitution) are closed in `cte_filter_phase_i6_string_minmax_f4.md`
- `cte_filter_phase_i6_string_minmax_fixups.md` — Phase I.6 S.1-S.6 hardening sweep (shipped): grouped result ownership (S.1), charset-aware multi-source merge (S.2), interpreter scratch-buffer guard (S.3), API builder validation rejecting unsupported string aggregates (S.4), RonSQL aggregate result printing (S.5), coverage extension (S.6).  Lands `testVarcharMinMax` and `ronsql_minmax_string` MTR
- `cte_filter_phase_i6_string_minmax_f4.md` — Phase I.6 F.4 (shipped): CTE delivery + linked-attr strings.  Substitutes string payload bytes for `accumulators[i].value` in `Dblqh::buildCteLinkedBuffer` (K.1), `Dblqh::emitCteGroupOutput` and remaining CTE emit paths (K.2/K.2b); loads linked string aggregate columns in `JoinAggInterpreter::kOpLoadCol` (K.3); redistributes string CTE aggregate slots (K.3b); adds block + MTR coverage (K.5 + R.1).
- `cte_filter_phase_i26.md` — **SHIPPED (2026-08-25, recorded green ×5 topologies after two first-record fixes: the sc-17 GB-first projection-ordering crash and the stale colvscol Test 9 pin)**: real-table vs CTE-output comparisons (`t.col > s.m`, the watermark shape pinned by the former sc-P1) on BOTH paths, via the CTE_LOOKUP jump-table filter with the real column as a linked parent projection.  Kernel W1: `Dbspj::cte_lookup_send` expands `m_attrParamPattern` whenever `T_ATTRINFO_CONSTRUCTED` (was agg-feed-only — the single kernel gap; ~10 lines).  API W2: new `NdbInterpretedCode::read_linked_column_to_reg` (one-word wrapper for the existing READ_LINKED_COLUMN_TO_REG opcode); RonSQL's I.3 col-vs-col arm upgraded from Bigint-only to typed registers (10 integer widths + Float/Double, mixed signedness via compareTypedRegs) — flips rpr-P1 → rpr-16/16b.  W3 fixes a LATENT WRONG-RESULTS bug: filter positions on a CTE op with attached linked projections were off by `num_linked_projs` (sc-17 pins the three-way base agreement).  W4 `classify_where_by_table` routes tree-ancestor-vs-CTE conjuncts to the CTE op's filter (`JoinOp::needs_parent_linked_attrs`; main scope only; gate keeps projection bookkeeping safe — no multi-leaf, aggregator only on the CTE op itself), with projection REGISTRATION deferred to `register_cte_filter_linked_projs` after load_join's GB block — first record caught a data-node crash (sc-17: `GroupByLinked` positions are a sequential counter, so GB projections must keep slots 0..n-1; classify-time registration shifted them and the target read an INT entry as its VARCHAR GB key — cmpVarchar require in GBHashTable::find); W5 `emit_child_ops` attaches projections without an aggregator; W6 ancestor-column atoms (col-vs-col typed, col-vs-const via `branch_linked_mem_*`, NULL guards ⇒ UNKNOWN rejects).  MTR: `body_passthrough_scalar_cte.inc` sc-11..18 + new sc-P1 (non-ancestor) + sc-P4 (DECIMAL) ×5; `body_root_pk_residual.inc` rpr-16/16b ×5.  Deferred: sibling-branch parents, CTE-root-with-real-descendant compares, cross-CTE compares, arithmetic sides
- `cte_filter_phase_e1.md` — Phase E.1: scanCte as main-query root (RonSQL)
- `cte_filter_phase_e1k.md` — Phase E.1K: kernel CTE virtual-column linked-attr support (sequenced after E.1, before E.2)
- `cte_filter_phase_e2.md` — Phase E.2: chained CTEs / CTE-of-CTE (RonSQL)
- `cte_filter_phase_j.md` — Phase J: LEFT-to-INNER promotion when WHERE rejects NULL on RHS (RonSQL)
- `cte_filter_phase_i1.md` — Phase I.1: IS NULL / IS NOT NULL on CTE column or aggregate output (kernel + RonSQL)
- `cte_filter_phase_i2.md` — Phase I.2: top-level OR / DNF in CTE_LOOKUP filter pushdown (RonSQL)
- `cte_filter_phase_i3.md` — Phase I.3: column-vs-column on CTE_LOOKUP (Bigint-only) (RonSQL + 1-line NdbInterpretedCode API)
- `cte_filter_phase_i4.md` — Phase I.4: CASE condition referencing a CTE column projection (kernel + RonSQL)
- `cte_filter_phase_i5.md` — Phase I.5: GREATEST / LEAST n-ary functions (v1 shipped: 2-arg col+const, RonSQL only)
- `cte_filter_phase_i5_v2.md` — Phase I.5 v2: register-based CASE codegen (v2a + v2b + v4 shipped: col-vs-col atoms in embedded CASE, n-ary GREATEST/LEAST via SVM extension, NULL propagation in v4)
- `cte_filter_phase_i5_v2b.md` — Phase I.5 v2b (shipped): n-ary GREATEST/LEAST via SVM extension (Greatest2/Least2 SVM ops; pair-op now expands to embedded normal-interpreter compare + Mov in programAggregator after Phase M; v2b also replaces v1's CaseExpr-based two-arg lowering)
- `cte_filter_phase_i5_v4.md` — Phase I.5 v4 (shipped, superseded by v7 for expression-local NULL handling): NULL propagation for nullable GREATEST/LEAST operands; parser-time nullable rejection removed
- `cte_filter_phase_i5_v5.md` — Phase I.5 v5 (shipped): new `READ_LINKED_COLUMN_TO_REG` kernel opcode; v2a's col-vs-col linked-side emission collapses from a two-word `READ_LINKED_TO_MEM + READ_*_MEM_TO_REG_CONST` sequence (zero-extended signed sub-Bigint) to one-word typed loading with correct sign extension; all 10 NDB integer widths on the linked side; leaf-side `READ_ATTR_INTO_REG` zero-extension on signed sub-Bigint queued as follow-up
- `cte_filter_phase_i5_v6.md` — Phase I.5 v6 (shipped): CTE linked-vs-linked GREATEST / LEAST runtime coverage; test-only — v2b's pair-op SVM path already handled CTE-leaf operands.  Surfaced unrelated planner gaps now tracked as I.16 / I.17
- `cte_filter_phase_i5_v7.md` — Phase I.5 v7 (shipped): expression-local NULL propagation in GREATEST/LEAST.  New `kOpSetRegNull` aggregation opcode + `NdbAggregator::SetRegNull`.  Pair-op nullable tail becomes `EmbeddedInterp(14) + Mov + Skip(1) + SetRegNull(dest)` (18 kernel words); only the immediately-following aggregate skips the row, unrelated `COUNT(*)` / `SUM` / `MIN` / `MAX` outputs still update
- `cte_filter_phase_i5_v3.md` — Phase I.5 v3a + v3b (shipped, `d52ce91428a`): float / double in col-vs-col CASE (v3a) and col-vs-const-float (v3b).  v3a kernel `handleReadLinkedColumnToReg` gained `NDB_TYPE_FLOAT` / `NDB_TYPE_DOUBLE` arms; RonSQL `can_load_integer_reg` → `can_load_typed_reg` accepts Float / Double; `compareTypedRegs` already promotes mixed int/float to double.  v3b lowers literals via `LOAD_DOUBLE_CONST` (Option A) reusing v3a's register-compare path — no new kernel work.  Surfaced a long-standing bug: CASE-condition args don't go through the general WHERE-expression `simplify_ce` pass, so negative literals arrive as T_NEGATIVE wrappers; fix calls simplify_ce in the col-vs-const pre-pass
- `cte_filter_phase_i16.md` — Phase I.16a + I.16b + I.16c (shipped, `318066232e9`): partial-key joins to multi-key CTEs.  I.16a guards `emit_child_ops`'s CTE_LOOKUP arm with a `groupby_columns` count check, throwing a clear permanent error when the join binds fewer keys than the virt PK requires.  I.16b/c add an AST-rewrite pre-pass in `load_join` that walks the joins list, finds the first INNER-join whose target is a multi-key CTE, and swaps it to root with the original parent demoted to a child.  Handles N-table chains and coexists with LEFT_OUTER joins ELSEWHERE in the chain.  LEFT_OUTER on the multikey-CTE-join itself stays on I.16a.  Non-root CTE_SCAN children (true I.16c per the original outline) deliberately not pursued — `scanCte()` in NDB API doesn't take a key array, so RonSQL follows API support.  **August 2026 addendum (`non_aggregate_phase_6.md`)**: the rewrite is extracted into `maybe_rewrite_partial_key_cte_root()` and also runs pre-gate in `parse()` for non-aggregate queries, lifting the pass-through gc-P1 restriction
- `cte_filter_phase_i9.md` — Phase I.9 (shipped): `scanIndex` inside a CTE materialisation subtree (testCteNdbApi.cpp Test 18 shape).  CTE bodies with WHERE bounds on an ordered indexed column now plan as `INDEX_SCAN + bounds` instead of `TABLE_SCAN + InterpretedCode filter`.  Adds per-CTE-body scan-config state on `QueryScope` (body_indexes / body_toplevel_conditions / body_scan_config_candidates / body_scan_config) separate from the main query's `m_*` fields, a new `select_cte_body_scan_config` helper invoked from `build_cte_scopes`, and a single-op INDEX_SCAN CTE-body emit branch that builds `NdbQueryIndexBound` + routes residual conjuncts to InterpretedCode filter.  Foundation for I.10 (MIN/MAX via DESC index + LIMIT 1).  MTR: `ronsql_cte_index_body.test` (equality, double-bounded, half-open + residual, no-WHERE regression)
- `cte_filter_phase_i10.md` — Phase I.10 (shipped, `2c5f5813f46`): scalar `MIN(col)` / `MAX(col)` CTE via ordered index + `maxRows=1`.  Detects scalar CTE bodies whose only output is direct-column MIN/MAX over a NOT NULL indexed column, emits ASC/DESC ordered index scan with maxRows=1, and falls back to baseline materialisation for unsupported shapes
- `cte_filter_phase_i11.md` — Phase I.11 (shipped): RonSQL SQL-level coverage for testCteNdbApi.cpp Tests 12-16.  Adds `ronsql_cte_kernel_t12_t16` covering `lookupCte` as CTE materialisation root + child, `lookupCte` as main-query internal node, `lookupCte` as CTE materialisation internal node, `scanCte` as main-query aggregate leaf, and `scanCte` as CTE materialisation root non-leaf.  Extends the non-aggregate parse gate only for projection-only real-table-root INNER join chains with complete-key CTE lookups.  Fixes `QueryPlanner` child-op initialisation so physical children cannot be misclassified as CTE ops during scoped resolution
- `cte_filter_phase_i12.md` — Phase I.12 (shipped): RonSQL SQL-level coverage for testCteNdbApiOuterJoin.cpp Tests 1/2/3/5/6.  Original I.12 target (CTE_SCAN as outer-join child cross-join) deliberately dropped at kernel level in `cte_outer_join_phase_3.md`; Phase G defensive reject preserved.  I.12 covers the shipped outer-join shapes — `scanCte INNER/LEFT JOIN readTuple`, `scanTable LEFT JOIN lookupCte`, scalar agg over `LEFT JOIN cte` with NULL-injection, and `scanCte` parent + main aggregator on real leaf.  Three RonSQL changes: non-aggregate gate accepts CTE-rooted projection-only chains and `LEFT_OUTER_JOIN` while keeping the complete-key CTE_LOOKUP coverage check; `print_passthrough_value` extended with `Char` / `Varchar` / `Longvarchar` arms mirroring the aggregator-path formatting; `execute_passthrough_drain` substitutes NULL into a transient `effective_attrs[]` for any column whose `NdbQueryOperation::isRowNULL()` reports the LEFT JOIN unmatched-row marker.  MTR: `ronsql_cte_outer_join.test`
- `cte_filter_phase_i8.md` — Phase I.8 (shipped, follow `cte_filter_phase_i.md`'s I.8 entry for commit hash): RonSQL non-aggregating join queries — `FROM real_table JOIN cte ON ... [WHERE pk = const]` projection-only (testCteNdbApi.cpp Test 17 shape).  Front-end gate relaxation, `emit_child_ops` ungated on `m_is_aggregate_query`, `execute_passthrough_drain` generalized to multi-op (pre-registers all virt-table cols on each CTE op in attrId order so the receiver buffer + `handle_rec_attrs` line up with the kernel-emitted row), TSV header deferred until first row so empty results match mysql client baseline
- `cte_filter_phase_i.md` — Phase I.7-I.15 unified index for "RonSQL doesn't yet emit shapes the NDB API already supports".  **I.7 shipped (`c2ce92bbd54`):** `lookupCte` as the main-query root (`SELECT … FROM cte WHERE pk = const`).  `emit_root_op` walks the WHERE for equality predicates on every virt-table PK column and, if all bound + no scan children, emits `qb->lookupCte(cteId, numCols, virtTab, const_keys, opts)` (testCteNdbApi.cpp Test 11 pattern); partial-key / no-WHERE / non-equality fall back to scanCte + inline filter.  Two follow-up fixes during landing: `0c72daaf55c` typed `constValue` dispatch (synthetic virt-PK cols can't go through `(void*, len)` overload — m_attrSize == 0); `a71f192cf04` populate `m_attrSize / m_orgAttrSize / m_arraySize` in `build_cte_virtual_tables` so the lookup-op `packed_rowsize → getSizeInBytes()` path delivers a non-zero result-buffer size.  The latter is now the canonical fix pattern documented in `project_synthetic_virt_table_gotcha.md`
- `cte_filter_phase_i17.md` — Phase I.17 (shipped, `8609cad17f4`): scalar aggregate CTEs end-to-end including the watermark / cross-CTE-join shape.  RonSQL relaxation accepts CTE bodies whose every output is an aggregate; kernel `Dblqh::continueJoinAggRedistribute` ships per-node `m_agg_results` to the DBTC-co-located owner via a `keyLen == 0` `JoinAggRedistributeReq`; `cteScanShouldEmitScalar` reduces to `refToNode(state->m_senderRef) == getOwnNodeId()`.  Top-level GREATEST/LEAST wrapped in implicit MAX; optional FROM clause auto-synthesised from qualified column refs; comma cross-join of two scalar CTEs uses the testCteNdbApi.cpp Test 20 pattern (lookupCte dummy-key + setParent on a 1-PK virt table)
- `cte_filter_phase_i17_redistribute.md` — Phase I.17e kernel companion to I.17: details the scalar redistribute integration (owner = DBTC's node via `m_senderRef`, `keyLen == 0` REDISTRIBUTE_REQ variant, new `mergeScalarAccumulators` helper)
- `cte_filter_phase_i18.md` — Phase I.18 (kernel + NDB-API shipped, `2784d7746e5`): typed normal-interpreter registers — full Option B retrofit (NULL/UNSIGNED/FLOAT byte flags + 64-bit value).  All producer/consumer handlers updated.  New opcodes `WRITE_REG_TO_MEM_ANY` (slot 62, type-agnostic 8-byte register-to-memory) and `LOAD_DOUBLE_CONST` (slot 45, IEEE-754 double immediate).  RonSQL MTR coverage queued as follow-up
- `cte_filter_phase_i19.md` — Phase I.19 (shipped, `eb041c0884e`): CASE literal normalisation and boundary fixes after the Phase I.5 v3b review.  Simplifies whole embedded CASE atoms before side resolution so literal-on-left forms like `100.0 < col` normalise like WHERE expressions; fixes signed INT literal bounds in `encode_constant()`; adds v3b MTR coverage for leaf FLOAT vs negative fractional literal, leaf DOUBLE, literal-on-left, and INT_MIN / INT positive-overflow boundaries
- `cte_filter_phase_i20.md` — Phase I.20 (shipped, `c3e050b1e17`): Phase I.16 follow-up for CTE lookup key coverage and rewrite validation.  Replaces key-count checks with ordered virtual-CTE-PK coverage checks; accepts/reorders or rejects full-key predicates in non-PK order; rejects wrong-column full-count joins; constrains the I.16c root rewrite to partial-key CTE joins whose parent alias is the original root
- `cte_filter_phase_i21.md` — Phase I.21 (shipped, `622b9cf82c3`): scalar CTE semantic guardrails after I.17.  Keeps the scalar dummy-key workaround local to scalar CTE child lookup; prevents root scalar CTE `WHERE` predicates from using keyless/dummy `lookupCte`; preserves nullable aggregate output metadata when the first scalar output can be NULL; constrains top-level implicit `GREATEST` / `LEAST` lowering to proven scalar-CTE expressions so ordinary row-wise table queries cannot silently become aggregate queries
- `cte_filter_phase_i22.md` — Phase I.22 (shipped, `e461c0c96ec`): DECIMAL MIN/MAX 64-bit range guard after Phase I.6 F.1.  Adds a RonSQL prepare-time rejection for scale-zero DECIMAL MIN/MAX declarations that can exceed the kernel's current `decimal2longlong` / `decimal2ulonglong` target range: signed precision > 18 and unsigned precision > 19.  Adds MTR coverage for safe boundaries and rejected unsafe declarations; full arbitrary-precision DECIMAL preservation remains out of scope
- `cte_filter_phase_i23.md` — Phase I.23 (shipped, `ffae6a9627a`): scoped CTE and stored-table name resolution.  Resolve CTE bodies in declaration order with only stored tables plus already-published CTE result interfaces visible; publish only each CTE name and exposed result columns to later scopes; reject ambiguous unqualified names across stored tables and visible CTEs; added `ronsql_cte_name_resolution` coverage.  Emit still uses compatibility arrays derived from scoped resolution
- `cte_filter_phase_i24.md` — Phase I.24 (shipped, `cf188857098` … `7e0f33b7890`): migrate emit paths from legacy parallel arrays (`column_attrId_map`, `column_map`, `column_table_idx`) to per-scope `ResolvedColumnRef` descriptors.  21-commit incremental migration covering CTE metadata helpers, filter emit, aggregation loads, pass-through routing, CASE condition emit, single-table aggregation, embedded filter loads, linked projections, subquery aggregation, aggregate validation, CTE source emit, column ownership, WHERE classification, result metadata, CTE metadata, and index-bound metadata.  Validation in `e8a2e00686a` followed by legacy-array removal in `7e0f33b7890`; I.11's kernel-topology coverage in `3ca4f897af3` runs on top of the migrated emit
- `cte_filter_phase_i25.md` — Phase I.25 (moved to Phase N): follow-up for two string CTE failures found while adding F.4 coverage.  Re-aggregating string CTE MIN/MAX through a CTE-root to non-unique ordered-index child join returned `NULL,NULL`, while the primary-key helper-table variant worked.  The intended `WHERE min_v < 'beta'` regression also needs quote-safe MTR compare handling plus clarified RonSQL string-literal parser semantics.  Also covers a CTE output comparison matrix for col-vs-col, const-vs-col, and col-vs-const across signed/unsigned integers, floats, DECIMAL widening, strings, exposed date/time types, and NULL semantics
- `join_root_index_scan_plan.md` — **Changes 1-4 shipped (MTR green ×5 topologies; Change 5 multi-op CTE bodies pending)**: main-query root of join queries emitted `scanTable` + filter unless the PK was fully equality-covered (`.explain_ronsql fs_batch` showed `[ROOT] TABLE_SCAN customer` despite a `c_custkey` range).  Root cause: `plan_index_and_filter` is `!is_join_query()`-gated and `emit_root_op`'s real-table branch had no range/secondary-index path.  Fix: Phase I.9's selector generalized to `select_root_scan_config` (main-scope call at end of `load_join`, `root_pk_equality_covered` guard, FORCE-INDEX bypass), shared `emit_index_scan_root` bound/residual emit used by both CTE bodies and the new `emit_root_op` INDEX_SCAN branch, EXPLAIN root CONDITIONS print, root index hints on join queries now honored (were silently ignored), generator hardened to reject non-constant right sides as bounds (latent col-vs-col `encode_constant` throw, also on the single-table path).  MTR family `body_main_root_index.inc` (mri-1..10 + P1) ×5 topology suites.  Change 5 (multi-op CTE bodies) pending
- `ronsql_orderby_limit_plan.md` — **Phase 0 IMPLEMENTED (2026-08-20, RONDB-1107); Phase 1 IMPLEMENTED (2026-08-21, pending rebuild + first `--record` ×5 suites — first record surfaced + fixed TWO engine defects: (a) mixed bare/qualified ORDER BY vs GROUP BY spellings got distinct parser col_idx entries and failed the printer's col_idx-equality GROUP BY check, fixed by `RonSQLPreparer::canonicalize_orderby_columns` resolved-identity rewrite before printer construction; (b) LIMIT 0 printed a bare TSV header where the mysql client prints nothing — long-standing, baked into the non-strict `ronsql_basic`/`ronsql_orderby_stress` baselines as recorded diffs — fixed by ResultPrinter header suppression on both the buffered and streaming paths, those two base-suite baselines re-recorded); Phase 2 SHIPPED (2026-08-21, all green on first record ×5 suites — LIMIT without ORDER BY accepted on all three projection-only gate shapes (CTE_SCAN root, single-table, join chains): streaming cutoff in `execute_passthrough_drain` (caller's `query->close()` = the I.14 early close) and `execute_single_table_passthrough` (scan arm `scanOp->close()`, PK arm LIMIT-0 suppression), LIMIT 0 = no output per the deferred-header convention; MTR `body_passthrough_limit.inc` pl-1..14 + P1/P2 ×5 suites incl. a LIMIT-260-of-300 batch-boundary early-close case; `body_passthrough_single_table.inc` st-P2 converted from LIMIT-rejection to ORDER BY+LIMIT-rejection, that family needs re-record); Phase 3 SHIPPED (2026-08-21, all green on record ×5 suites — projection-only ORDER BY [+ LIMIT] via buffered client-side sort: rows buffered as `NdbRecAttr::clone()` arrays (RAII-freed; keeps ResultPrinter formatting-only), sort keys matched to stored slots by `same_resolved_column` identity with extra getValues for non-projected real columns / virt-fetch reuse for CTE columns, `NdbSqlUtil` m_cmp + charset comparator with NULLs-first-ASC + LEFT-JOIN-NULL rows as NULL, `std::partial_sort` top-N, 1M-row/256MB cap as permanent error; MTR `body_passthrough_orderby.inc` po-1..14+P1 ×5; first record surfaced + fixed a third resolution defect — a bare ORDER BY name sharing its registry entry with a bare COLUMN output got alias-marked and the scoped resolver classified the shared entry AliasOnly before column resolution, breaking the output itself (fix: skip the mark when the matched output is a COLUMN with the same col_idx); pl-P1/P2 + st-P1/P2 ORDER-BY-rejection probes retired (both families re-recorded); CLI: `fs_topk` restored to projection-only form, `fs_history` un-flagged — Phase 4's unlock role collapsed into Phase 3); **Phase 4b SHIPPED (2026-08-25, recorded green ×5 suites + regression pass; pre-existing `_orderby`/`_limit`/`_single_table`/`ronsql_index_hints` baselines unmoved)** — ORDER BY served by ordered-index scan order on the single-table pass-through path: `add_orderby_scan_config_candidates` tags a candidate whose index delivers the ORDER BY (list = index columns after any leading equality-bound columns, uniform direction) with `ScanConfig::index_order` + tie-break bonus (WHERE-first), pushes bonus-only candidates for order-serving indexes without bounds (beats the table scan), FORCE/USE/IGNORE honoured with the FORCE satisfiability check deferred past the ORDER BY pass; `open_single_table_scan_op` sets `SF_OrderBy [| SF_Descending]` (API merge of the fragment scans) and the drain streams through the Phase 2 LIMIT cutoff instead of buffering; EXPLAIN `ORDER BY: index order ...` vs `client-side sort ...`; MTR `body_passthrough_orderby_index.inc` poi-1..18 + P1/P2 ×5 (FATAL EXPLAIN greps + runtime pins of the `x-ronsql-phases` `rows=` counter via the new `suite/ronsql/include/ronsql_phase_rows.inc` — index-order cases drain exactly min(LIMIT, matches), client-side fallbacks drain every matching row); CLI `fs_latest` added (fs_history stays buffered — its WHERE index is not in date order); Phases 5-6 partially delivered / deferred**: ORDER BY / LIMIT support across RonSQL query shapes.  Inventory: aggregate queries (single-table AND join/CTE) already share the full ResultPrinter sort+limit machinery (charset-aware `compare_rows`, buffered `print_result_ordered`, LIMIT truncation) but only single-table is MTR-tested; projection-only passthrough shapes reject ORDER BY/LIMIT at the parse gate (RonSQLPreparer.cpp:569/:588); single-table non-aggregate SELECT is wholly unsupported; CTE-body / subquery ORDER BY+LIMIT WERE parsed but **silently ignored** (wrong results — correctness bug).  Phases: 0 reject body/subquery ORDER BY+LIMIT (**SHIPPED**: `reject_ignored_orderby_limit` from `analyze_ctes` / `analyze_subqueries_ce` scalar+IN+EXISTS arms (original statements, pre-rewrite) / `analyze_select_subqueries`; MTR `ronsql_cte_dd_orderby_limit_reject` obl-1..7; audit found no existing test or RonSQL-enabled benchmark in body/subquery position); 1 MTR-verify aggregate join/CTE ×5 topologies (**SHIPPED**: `body_orderby_limit.inc` + `ronsql_cte_dd_orderby_limit.test` ob-1..22 + ob-P1..P3 — ORDER-verifying strict diffs via `$skip_sort=yes` with total-order tie-breakers, unlike the sorted-compare families; covers GROUP BY-col + aggregate-alias ordering incl. string/DATE/DATETIME/TIMESTAMP(6) aggregate results, LIMIT 0/N/>set + the streaming no-ORDER-BY cutoff, fs_topk shape + EXPLAIN greps, two-CTE, HAVING+ORDER BY+LIMIT, scalar-CTE cross-join + LIMIT, qualified ORDER BY on a CTE virt column; `ORDER BY SUM(...)` pinned as a syntax error — alias form only; stale body_mainmode.inc deferral markers rewritten); 2 streaming LIMIT + early close for passthrough; 3 buffered client-side sort (StoredPassthroughRow, NdbSqlUtil cmp, memory cap) unlocking `fs_topk`; 4 single-table non-aggregate SELECT with optional SF_OrderBy index-order top-N unlocking `fs_history`; 5 flip CLI MySQLOnly flags + add official ORDER BY to `cte_tpch_q*` (**partially delivered early**: fs_topk rewritten to aggregate form + unlocked, cte_tpch_q2/q22 gained ORDER BY, all 5 tpch_q*_official audited as blocked beyond ORDER BY/LIMIT); 6 deferred (positional/expr ORDER BY, OFFSET, distributed top-N)
- `non_aggregate_pushdown_plan.md` — **PLAN (not yet implemented)**: non-aggregate query pushdown in five phases — (1) single-table PK lookup / ordered index scan / table scan via the plain NdbScanOperation path (reusing `plan_index_and_filter`, new non-aggregate drain, PK-lookup ScanConfig variant), (2) real-table snowflake pushed joins (2a PK/UK-lookup children INNER, 2b index-scan children = MultiScanQuery/REPEAT_SCAN_RESULT, 2c LEFT OUTER) by dropping the `query_has_cte` requirement in the parse gate (RonSQLPreparer.cpp:694-695), (3) star-schema fan-out on a common key (3a lookup children, 3b bushy scan children — non-aggregating sibling of `star_schema_plan.md`, no multi-leaf state needed), (4) scalar aggregate CTEs (accept `ScalarDummy` coverage in the gate + condition routing to CTE_LOOKUP jump-table filters), (5) GROUP BY CTEs mixed freely into snowflake/star trees.  Verified: Steps 1-3 need zero kernel/NDB-API work (legality envelope table with NdbQueryBuilder/DBSPJ refs in the doc); QueryPlanner/emit/drain are already general — the parse gate is the main blocker.  Phase 0 pre-work: `emit_root_op` PK-covered branches silently drop residual WHERE conjuncts (:7127/:6960 — live wrong-results bug), and `print_passthrough_value` lacks temporal/DECIMAL arms + ColumnMetadata.  Composes with `ronsql_orderby_limit_plan.md` (whose Phase 4 it subsumes minus ORDER BY/LIMIT); supersedes `ronsql_join_phase7.md` Step 45.  MTR: five new `nonagg_*` data-driven families ×5 topologies, sorted compare, mandatory multi-batch scan-scan cases
- `non_aggregate_phase_0.md` — **IMPLEMENTED (August 2026, pending user build + MTR --record)**: detailed Phase 0 of `non_aggregate_pushdown_plan.md` — 0a extends `collect_pk_equalities` with consumed-conjunct tracking + shared `build_root_residual`, then applies residual WHERE conjuncts in the three silent-drop `emit_root_op` branches (real `readTuple` root: NdbScanFilter interpreted code with the 64-word lookup cap + cascade fallback to PK-ordered-index scan → scanTable; PK-covered + scan-child `scanIndex` root: unconditional residual filter mirroring `emit_index_scan_root`; full-key `lookupCte` root: residual via `emit_cte_lookup_filter`); fixes contradictory-PK-equality wrong results as a side effect.  0b plumbs col_idx-indexed `ColumnMetadata` into the passthrough ResultPrinter ctor (factored `build_result_column_metadata`), extracts `print_temporal_packed` from `print_aggregate_result`, and extends `print_passthrough_value` with packed-Bigunsigned CTE temporal decode + raw Date/Year/Datetime2/Time2/Timestamp2/Decimal arms (quoting per GROUP BY-print conventions; Bit/binary/blob/old types stay cleanly rejected).  MTR: failing-first families `body_root_pk_residual.inc` (r1-r8) + `passthrough_types.inc` (t1-t7) ×5 topologies.  First record of rpr-1 exposed a **pre-existing data-node crash**: aggregate no-CTE queries with a PK-equality-covered root emitted a readTuple root = lookup-rooted TCKEYREQ query, but DBTC JOIN_AGG_SETUP is SCAN_TABREQ-only → DBSPJ `lookup_send` `ndbrequire(m_aggNodes.get(nodeId))` (DbspjMain.cpp:8748) on empty `m_aggNodes`; fixed by `emit_root_op` `lookup_root_supported` gating (scan fallbacks unless pass-through or main-scope-with-CTE) + prepare-time `QRY_WRONG_OPERATION_TYPE` in `NdbQueryDefImpl` for main-agg no-CTE non-scan-rooted defs + kernel hardening per `tiered_response_policy.md` (the tree bits are API-controlled: `validateAggregateFlags` now fails the query with `InvalidAggregateFlags` when `RT_AGGREGATE` is set with empty `m_aggNodes`, and the LqhKeyReq `do_init` gains the missing `m_aggNodes.clear()` — TransientPool Requests carried stale bits); runtime `ndbrequire(m_aggNodes.get(nodeId))` sites stay as trusted-tier (DBTC-data) invariants; rpr-13/14 cover the still-legal CTE-containing readTuple-root + residual shape
- `non_aggregate_phase_0.html` — Visual companion to `non_aggregate_phase_0.md` ("Root Plan Cascade", SVG diagrams): the emit_root_op root-plan decision cascade with the Phase 0a additions highlighted, the bounds-prune-reads vs filters-prune-transfer pipeline, the scan-vs-lookup protocol split behind the DbspjMain.cpp:8748 crash with the three-layer defense, per-shape optimality verdicts (the named suboptimal case: aggregate no-CTE + hash-PK full-key ⇒ scanTable), the two open gaps (lookup-protocol JoinAgg setup; EXPLAIN not showing emit-time root refinement), 0b decode-path table, and the rpr/pt test-coverage map
- `non_aggregate_phase_1.md` — **IMPLEMENTED (August 2026, pending user build + MTR --record)**: detailed Phase 1 of `non_aggregate_pushdown_plan.md` — single-table non-aggregate queries on the plain NDB API path (no NdbQueryBuilder/SPJ hop).  W1 parse-gate shape C (`projection_only_single_table`, no CTEs, FROM required); W2 PK-lookup detection in `plan_index_and_filter` (`m_pk_lookup` + consts, `!m_is_aggregate_query`-guarded — single-table twin of the Phase 0 lookup-root rule); W3 `execute_single_table_passthrough()` with a readTuple lookup arm (`NoDataFound` ⇒ empty result) and a scan drain arm reusing ScanConfig bounds/filter + the Phase 0b passthrough printer; W4 shared scan-setup refactor; W5 EXPLAIN PK-lookup access line; W6 `body_passthrough_single_table.inc` ×5 topologies.  v1 policy: PK lookup only when the WHERE is fully consumed as keys — PK+residual takes the scan fallback because the RecAttr-style NdbOperation read has no interpreted-code facility (`OO_INTERPRETED` is NdbRecord-only); the NdbRecord interpreted read has since shipped as `non_aggregate_pk_residual_lookup.md` (see its entry below).  LIMIT streams with early close and ORDER BY sorts client-side or, when an ordered index serves it, streams in index order via SF_OrderBy (`ronsql_orderby_limit_plan.md` Phases 2+3+4b; `fs_history` unlocked buffered, `fs_latest` index-ordered)
- `non_aggregate_pk_residual_lookup.md` — **SHIPPED (2026-08-25, recorded green ×5 topologies)**: the Phase 1 v1-policy follow-up — single-table PK-equality-covered WHERE with residual conjuncts executes as an NdbRecord `readTuple` + `OO_INTERPRETED` filter + `OO_GETVALUE` RecAttr reads instead of the scan fallback (a full table scan on hash-PK tables).  `detect_pk_lookup` classifies conjuncts key-vs-residual (`m_pk_lookup_cond_map`) and gates via a plan-time trial build (shared 64-word `LOOKUP_FILTER_MAX_WORDS` cap hoisted from the Phase 0 join-root site; throws on unsupported types caught → scan fallback, keeping error timing identical incl. EXPLAIN); the residual execute arm keeps the RecAttr printer untouched (extra gets return plain `NdbRecAttr*`); interpreted-reject surfaces as 626 = NoDataFound == row-absent by design, so the existing NoDataFound ⇒ empty-result handling covers it with zero change; duplicate/contradictory PK equalities become key+filter (correct empty result).  EXPLAIN prints `CONDITIONS (k keys and m filters):` with `KEY[ordinal]:`/`FILTER:` labels.  MTR: st-4/st-5 flip to fatal EXPLAIN greps + `ronsql_phase_rows.inc` pins; new st-15..22 incl. over-cap (st-19), unsupported-type TIME(3) fallback (st-20), and local VARCHAR-PK `vkey1` (st-21/22 — NdbRecord key_row length-prefix encoding is the one new byte-level path)
- `non_aggregate_phase_2.md` — **IMPLEMENTED (August 2026, pending user build + MTR --record)**: detailed Phase 2 of `non_aggregate_pushdown_plan.md` — real-table snowflake pushed joins, projection-only, no CTE required.  W1 drops the parse gate's `query_has_cte` conjunct (flag renamed `projection_only_join_chain`; CTE shapes keep their exact envelope); W2 adds two `QueryPlanner::plan` pre-checks turning runtime NDB 48xx errors into clean prepare-time errors for aggregate and pass-through joins alike — linked-column identity (type/precision/scale/length/charset, BLOB/TEXT rejected, unknown parent column named) and the internal-node budget (unique-index lookup counts as 2 toward the 32-node cap); everything downstream (planner classification, Phase-0-hardened emit, multi-op pass-through drain with isRowNULL substitution) already handled real-only chains.  2a lookup children (green path), 2b INDEX_SCAN children (MultiScanQuery/RT_REPEAT_SCAN_RESULT), 2c LEFT OUTER incl. nullable join keys; the sn-15 INNER-below-LEFT probe CONFIRMED wrong results on first record (NULL-extended rows delivered that the INNER must eliminate) — the gate now tracks under-LEFT aliases and rejects INNER-below-LEFT with a targeted error until nest metadata (`setFirstInnerJoin`/`setUpperJoin`) is emitted, and the same shape on the AGGREGATE path is a suspected latent bug (probe in findings); sn-4/5 are the first pure LookupQuery pass-through queries.  MTR `body_passthrough_snowflake.inc` (sn-1..15 + P1..P3, local `snow1` unique-index child) ×5 topologies; deferred: nest options, `ronsql_large` multi-batch scan-scan, semi/anti revival (child constant bounds since DONE — see the child_bounds entry)
- `join_nest_semantics_plan.md` — **Parts A + B COMPLETE (B pending user build + re-record).  A: aggregate-path INNER-below-LEFT CONFIRMED WRONG (ns-1 recorded COUNT(*)=30 vs MySQL 10 — miss AND NULL-key rows both counted; injection behavior differs by aggregate-leaf position vs the green testMultiOuterJoinAggNdbApi Test 2 — noted for future genuine-nest work).  B: `promote_left_joins()` backward-pass in parse() rewrites every flat-chain INNER-below-LEFT to all-INNER (left-associative SQL + null-rejecting equality join conditions; one pass exact since ON conditions reference only earlier aliases); gate check demoted to defensive; ns-1..4 + sn-15 re-enabled as compares, new ns-8 fixpoint-cascade case, EXPLAIN greps pin [INNER]/[LEFT JOIN].  Part C (nest metadata) deferred until parenthesized join grammar exists.**  Verification + fix plan for the Phase 2 sn-15 INNER-below-LEFT finding.  Analysis reframes it: SQL's left-associative `A LEFT B JOIN C ON C.ref(B)` means `(A LEFT B) INNER C` ≡ all-INNER (null-rejecting equality), while RonSQL's tree expresses `A LEFT (B INNER C)` — DBSPJ answered the wrong question correctly; nest metadata (`setFirstInnerJoin`/`setUpperJoin`) is for genuine parenthesized nests RonSQL cannot parse.  Evidence: `testMultiOuterJoinAggNdbApi` Test 2 proves the AGGREGATE path green for the lookup-MISS sub-case with the identical tree; open cells are aggregate×NULL-key and row-path×miss.  Part A `body_nest_semantics.inc` (ns-1..7, local `nestp` table with matching/dangling/NULL refs) closes the open cells by recording — runnable today since aggregate queries skip the gate; Part B = join-condition LEFT→INNER promotion pre-pass in parse() (fixpoint cascade; Phase J `:1600` precedent; fixes both paths, demotes the Phase 2 gate rejection to defensive, re-enables sn-15); Part C = nest-metadata emission design, deferred until the grammar gains parenthesized join expressions
- `non_aggregate_phase_3.md` — **IMPLEMENTED (August 2026, pending user build + MTR --record)**: detailed Phase 3 of `non_aggregate_pushdown_plan.md` — star-schema fan-out for projection-only queries (3a lookup-children stars incl. self-join stars; 3b bushy PK-prefix INDEX_SCAN siblings = cross product per parent row under RT_REPEAT_SCAN_RESULT).  Mostly a test phase — fan-out was already structurally admitted end to end — plus two gaps found while planning: `QueryPlanner` alias-uniqueness validation (MySQL ER_NONUNIQ_TABLE parity; duplicate aliases previously bound silently to the first match, aggregate joins included) and the EXPLAIN join-plan `<- parent` topology annotation (a star and a chain printed identically; baseline-safe — no result file records the print).  MTR `body_passthrough_star.inc` (sr-1..10 + sr-P1; local star_e/m/v tables give the ordered-PK-prefix bushy shapes the hash-PK shared schema cannot) ×5 topologies; deferred: `ronsql_large` bushy multi-batch, aggregate star (RONDB-1044) (branch-constant bounds since DONE — see the child_bounds entry)
- `non_aggregate_phase_4.md` — **IMPLEMENTED (August 2026, pending user build + MTR --record)**: detailed Phase 4 of `non_aggregate_pushdown_plan.md` — scalar aggregating CTEs comma-cross-joined into projection-only queries.  Research reshaped the parent sketch: scalar-CTE joins are the grammar's conditionless comma production only (`JOIN s` without ON is a syntax error; `LEFT JOIN <scalar cte>` inexpressible); a scalar CTE always emits exactly one row so the cross join is a 1-row multiplier; and real-vs-scalar comparisons (`t.col > s.m`) were missing on BOTH paths — since SHIPPED by `cte_filter_phase_i26.md` (see its entry).  W1 = two gate changes (conditionless joins admitted only for scalar-CTE operands + ScalarDummy in the coverage accept-list); W2 = ScalarDummy `setParent(tree_parent_op_idx)` fix (old `setParent(root)` clobbered the planner's sibling chain for 3+ comma CTEs on either path — sc-8 is the first-ever 3-CTE probe); emit/drain/printer needed zero changes (ScalarDummy arm is aggregation-independent, scalar virt PK is a nullable PK, metadata plumb kind-agnostic).  MTR `body_passthrough_scalar_cte.inc` (sc-1..10 + 5b + P1..P3) ×5 topologies.  Landed as `54f2214e9ed` + baseline refresh `958b0e440a7`; sc-6 surfaced a kernel defect on first record (scalar CTE_LOOKUP REFed GROUP_NOT_FOUND on empty input, dropping cross-join parent rows on BOTH paths — sc-10 pins the aggregate twin), fixed by emitting the Init-prepared `m_agg_results` (COUNT=0, others NULL) in the DblqhMain scalar CTE_LOOKUP arm
- `non_aggregate_phase_5.md` — **SHIPPED (August 2026, all green on first record ×5 topologies)**: detailed Phase 5 (final) of `non_aggregate_pushdown_plan.md` — grouped (GROUP BY) CTEs joined anywhere into projection-only snowflake/star trees, the general form of I.8/I.11/I.12's fixed shapes.  Audit found ZERO code changes needed: the gate already accepts grouped complete-key CTE_LOOKUPs (`ExactOrdered`/`ExactPermuted`), planner sibling re-chaining is type-general, the Phase 2 linked-column identity check skips CTE-linked ops (`table != NULL` guard), mixed-parent ON conditions already reject cleanly, and the D5 `linked_source_is_leaf_ancestor` fan-out rejection is aggregation-only — so the CTE-branch + real-branch star (gc-3) is legal on the pass-through path.  Pure coverage phase: MTR `body_passthrough_groupby_cte.inc` (gc-1..13 + P1..P3, local 2-col `gckey` table for multi-key CTE joins) ×5 topologies, incl. three first-ever-on-either-path probes — gc-7 two grouped CTEs on different parents (separate branches), gc-8 real PK lookup below a CTE_LOOKUP keyed on its output, gc-9 CTE_SCAN root + two-level real chain below.  Kept restrictions pinned at the time: partial-key CTE joins and multi-alias multi-key CTE keys — **both since LIFTED by `non_aggregate_phase_6.md`** (see its entry) — and non-aggregating CTE bodies (still rejected, kernel-facing)
- `non_aggregate_phase_6.md` — **SHIPPED (2026-08-25, recorded green ×5 topologies)**: pass-through CTE-join envelope — lifts Phase 5's two kept restrictions, RonSQL-only.  6a (gc-P1): the I.16b/c partial-key-CTE root rewrite is extracted into `maybe_rewrite_partial_key_cte_root()` and called from `parse()` BEFORE the projection-only gate, so the gate evaluates the post-rewrite tree (CTE_SCAN root + demoted keyed child = the shipped gc-9 shape — zero gate relaxation); demoted-root copy clears index hints; residual Partial/WrongColumns states throw the clean I.16a/I.20-mirror messages.  6b (gc-P2): per-key parent sources — `JoinOp::key_parent_op_idx[]`, QueryPlanner resolves every ON condition's parent with effective parent = deepest key source (all sources must lie on one ancestor chain, mirroring `linkWithParent`'s rule; clean planner error otherwise), per-key Phase 2 type-identity check, emit links each key from its own source op, EXPLAIN Key: line prints per-key sources (byte-identical for single-parent plans); applies to BOTH paths (aggregate twin gc-20).  MTR `body_passthrough_groupby_cte.inc` gc-14..20 + gc-P1a/b/c + gc-P2b ×5 topologies.  Deferred: multi-alias partial-key CTE joins, sibling-branch key sources, LEFT-on-partial, CTE-on-CTE root rewrite
- `child_bounds` feature (no own plan doc — landed against `next_steps.md` join-root follow-ups items 2+3; findings in `mysql-test/suite/ronsql_cte/findings/child_bounds.md`) — **SHIPPED (2026-08-25, recorded green ×5 topologies)**: child-local constant WHERE conjuncts on non-root INDEX_SCAN join children emit as constant index range bounds after the linked join-key prefix, on the aggregate, pass-through AND CTE-body-child paths.  `assign_cross_table_index_bounds` generalized to `assign_child_index_bounds(QueryScope&)` (cross-table matching factored into `cross_table_bound_op`; const eligibility = the root generator's set via `child_const_bound_op`, NOT NULL columns only in v1; per-column low+high pairs and EQ-continue like the root generator; consumed conjuncts leave `join_where_ce[op]`); `RangeBound::const_cond` carries the conjunct; emit memoizes constValue operands so EQ pairs collapse to BoundEQ; best-child-index re-selection via `QueryPlanner::collectOrderedIndexCandidates` + scoring (EQ=3/range=1, strictly-better switches, ties keep the planner choice).  TWO latent bugs fixed: kernel `Dbspj::scanFrag_fixupBound` renumbering stamped upper-only bound tails (EQ,...,EQ,GE) with the previous column's id (also reachable via untested cross-table T_LT/T_LE); and `QueryPlanner::plan`'s index matchers accept PERMUTED join-key sets while emit binds positionally — keys now normalized to physical index/PK column order with an exact bijection check.  EXPLAIN: `Bounds:` prints const values; new per-op `Residual filter: yes` line.  NEW root-path correctness suspicion recorded in next_steps.md (root bounds lack a nullability guard).  MTR: `body_child_bounds.inc` cb-1..12 ×5 topologies (fatal greps from day one) + sr-6 EXPLAIN pins (star family re-record); regression must include the mysqld `ndb` pushdown suites (fixupBound is shared)
- `local_execution_mode_plan.md` — **PLAN (not yet implemented)**: RonSQL `LOCAL` execution mode — `[EXPLAIN] [LOCAL] [WITH x AS ([LOCAL] SELECT …)] SELECT …` places all SPJ root-fragment work on own-node DBSPJ instances and a single aggregation state (per main query / per LOCAL CTE) on the TC node, making JOIN_AGG_SETUP/COMPLETE/RELEASE node-local and skipping the CTE redistribute + FINAL_REP barrier; **row reads stay unrestricted** — rows read on other nodes ship to the TC-node state via a new networked agg feed (GSN_JOIN_AGG_FEED_REQ, composed from the cteLookupAggFeed / buildCteLinkedBuffer / loadColumnTypedFromBuf precedents, completion barrier via the dormant expectedOpCount + m_completed_ops pair).  Flag rides the next free extended-requestInfo bit in storedProcId upper 16 bits (in-flight work, bits 31/30 taken) + LOCAL_MODE_FLAG in JoinAggSetupReq.  No standard SQL exists for placement; LOCAL keyword already reserved in RonSQL.  Phases: 0 transport/API, 1 placement + local signals (interim ZLOCAL_AGG_REMOTE_READ 1271 gate), 2 RonSQL syntax + EXPLAIN `[LOCAL]` + MTR ×5 topologies, 3 networked feed (core), 4 per-CTE LOCAL + benchmarks
- `aggregation_treenode_alternative_plan.md` — **PLAN / INVESTIGATION**: alternative feed mechanism for `local_execution_mode_plan.md` Phase 3 — aggregation as an explicit QueryTree node (`QN_AGGREGATE`, op 0x9): the last table becomes a normal internal node (NI_LINKED_ATTR projection, rows flow to DBSPJ), a new synthesized leaf TreeNode feeds the DBLQH JoinAggregationState via JOIN_AGG_FEED_REQ (SPJ→DBLQH, node-local under LOCAL).  Modeled on the cte_lookup TreeNode precedent (g_CteLookupOpInfo, joinAggStateKey CONF-only feed, m_outstanding structural barrier — no counting protocol, no read-set machinery, reading-side hot paths untouched).  Orthogonal to LOCAL: routing entry selects single-state / per-node states / future hash-routed feeds (kernel-complete GROUP BY).  Recommended adoption: LOCAL-only emission first (distributed keeps in-place handleJoinAggRow); simplifies outer-join NULL rows, CTE indirect feed, multi-leaf mapping.  Cost: last-table rows transit SPJ (+1 local hop under LOCAL; real regression only if distributed mode ever migrates)
- `frags_per_worker_plan.md` — **Phases 1-8 implemented (wire + gate + NdbQueryOptions setter + prepareSend/SCAN_TABREQ + DBTC chunked SPJ-instance assignment w/ budget-guard fallback + DBSPJ CTE-root multi-fragment scan + compareFragLocation fragId tiebreak for root-CTE_LOOKUP exactly-once + RonSQL `[EXPLAIN] [FRAGS_PER_WORKER = <n>] SELECT` syntax + tests: testJoinAggNdbApi Test 23 K-sweep + `ronsql_cte_dd_frags_per_worker` ×5 topologies w/ new `$RONSQL_PREFIX` in ronsql_compare.inc; MTR result files need first --record). fs_batch K=4 measured ~10% over K=1; scanCte-containing queries stay pinned K=1 (cte_scan_start virtual-fragment→node mapping keys on single m_rootFragId)**: configurable fragsPerWorker (1/2/4/8) for JoinAgg pushed queries incl. CTE compound queries — targets the fan-out fixed cost from `next_steps.md` "Reduce Fixed Overhead" item 3 (agg queries force `m_fragsPerWorker = 1`).  Knob: `NdbQueryOptions::setFragsPerWorker` on the root scan op def + RonSQL `[EXPLAIN] [FRAGS_PER_WORKER = <n>] SELECT` statement-head syntax; wire = log2(K) in SCAN_TABREQ storedProcId bits 16-17 (extended-flags area, `ScanTabReq::get/setFragsPerWorker`, JoinAgg-only, gated by `ndbd_support_joinagg_frags_per_worker` @ 26.04.2), with `scanParallelism` re-defined as worker count (bit-identical when K=1).  Kernel change surface is small because DBTC bundling is purely primaryBlockRef-driven: chunk each node's fragments into K-sized groups with distinct SPJ instances in the MultiFrag assignment block (DbtcMain.cpp:17672-17725) and the existing qsort+bundling emits K-frag SCAN_FRAGREQs unchanged; JoinAgg+MultiFrag 3-section wiring already exists from Phase 7.  DBSPJ main root already accepts fragment subsets; CTE enablement (own commit) = CTE-root scans the request's full fragId list instead of single `m_rootFragId` (DbspjMain.cpp:10888-10951) + relax the DBTC `scanParallel < tfragCount` CTE abort to a coverage check (:17473); CTE completion barrier is already per-worker (`registerCteScanFragHandle`).  Key risk: bundle/receiver deficit hangs JoinAgg (PassAllConfs, no NEXTREQ) — DBTC budget-guard falls back to legacy assignment.  Default stays 1 (opt-in); default-flip decided after fs_batch/offline_fs_* benchmarking
- `cte_finish_index_hints.md` — CTE finish point (W0–W3): shared scan-config candidate generator; composite multi-column index bounds in CTE bodies (with an NDB-API `appendBoundValue` attrId fix for the 4259 duplicate-attr bug on CTE-materialization root scans); OR/DNF in CTE-body WHERE; MySQL-style `FORCE`/`USE`/`IGNORE INDEX` hints (root scans only, joined-table hints rejected).  Documents the I.16 auto-rewrite as the one sanctioned exception to query-defined join order.  MTR: `ronsql_cte_composite_index`, `ronsql_cte_or_body`, `ronsql_index_hints`
- `cte_filter_phase_n.md` — Phase N (completed final wrap-up): closes or explicitly defers the last RONDB-1050 catalogue items.  I.25 string CTE query failures and comparison matrix shipped; I.13 SQL-facing batch-size control is deliberately deferred as NDB-API-only for RONDB-1050; I.14 RonSQL LIMIT-aware early close is deferred to broader LIMIT / ORDER BY pushdown while kernel/API early close remains block-tested; I.15 multi-fragment / multi-node CTE behaviour is documented in `cte_filter_phase_n4_coverage_audit.md`
- `cte_test_driven_findings.md` — **Test-driven CTE phase** next-phase backlog: a realistic data-rich MTR suite (`mysql-test/suite/ronsql_cte/` + topology siblings `ronsql_cte_ng1r3/ng2r2/ng2r3/ng4r2` at 3/4/6/8 data nodes) sweeping aggregating-CTE shapes RonSQL-vs-MySQL. Surfaced 17 gaps/bugs (numbered D1–D22; D7–D9/D13/D14 retired as SUM-over-DECIMAL artifacts) incl. 3 data-node crashes (D6 DBSPJ assert 2343 high-cardinality lineitem CTE; D18 string MIN/MAX re-aggregation Error 6000; D22 wrong COUNT under 6/8-node redistribution), 6 hangs (D2 COUNT(col), D3 projection-only-over-lookup, D4/D12 col-vs-col, D5 3-table real-cte-real chain, D19 main-WHERE-on-parent, D20 multi-key lookup), and errors/format gaps (D1 SUM-over-DECIMAL, D10 IS NULL in CTE body, D11 GREATEST/LEAST in CTE-body WHERE, D15 DECIMAL MIN/MAX scale, D16 scalar COUNT-over-empty=NULL, D17 DATE MIN/MAX). Confirmed green envelope (~81 cases ×5 topologies) is the regression net. Crash traces in `mysql-test/suite/ronsql_cte/findings/crash_artifacts/`; per-shape detail in `findings/_discovery_log.md`.  **All D-findings are now CLOSED — see the `cte_fix_plan.md` entry below for the per-item resolution summary.**
- `cte_test_authoring_guide.md` — Authoring guide for the data-driven CTE suite: schema/data semantics, harness conventions (ronsql_compare.inc, AS-aliases, QUERY_FILE for string literals, ronsql_explain.inc), and the corrected supported/forbidden envelope used to build the families
- `cte_fix_plan.md` — Remediation plan for the test-driven findings, sequenced hangs → crashes → wrong results → remaining errors. Per-item repro (disabled case + `# NEXT-PHASE` marker), suspected source area (e.g. DbspjMain.cpp:7825 for D6, RonSQLPreparer.cpp:7069 for D1, :5934 for D10), and re-enable steps. Phase 1 hangs (D2/D3/D4/D5/D12/D19/D20), Phase 2 crashes (D6/D18), Phase 3 wrong-results (D15/D16/D21/D22), Phase 4 errors (D1/D10/D11/D17). **Status: the ENTIRE D-catalogue is CLOSED.** Fixed: D1/D6/D10/D11/D15/D16/D17/D18/D21/D22/D23/D25/D26 (D17 = DATE MIN/MAX, see `cte_date_minmax_plan.md`; D6 = metadata-through-JOIN_AGG_SETUP series + CTE-completion heartbeat, which also closed D22; D25 = `senderAggStateKey` round-trip; D26 = strnxfrm hash-scratch race).  Resolved without a code defect: D2 (was D3), D19/D20 (stale-binary artifacts; D20's re-enable later surfaced the real D26).  Cleanly REJECTED at prepare, feature deferred: D4/D12 (CTE-body col-vs-col WHERE), D5 (fan-out aggregation across a real-table sibling — `linked_source_is_leaf_ancestor` guard).  Remaining work in that doc is the "Deferred feature work" list, not bugs.
- `cte_date_minmax_plan.md` — **D17 (SHIPPED)**: MIN/MAX over a DATE column in a CTE via the integer-day shortcut. As shipped, DATE is handled exactly like `MEDIUMUNSIGNED` at the numeric level (3-byte `uint3korr`, `IsUnsigned`→true, `AlignedType`→BIGINT) so the result returns as **Bigunsigned** with no new wire format; RonSQL tags the output via `ColumnMetadata::is_date` (sourced through the existing `resolve_cte_output_columns_for_scope` plumb-back) and the `ResultPrinter` unpacks `w`→`YYYY-MM-DD`. Kernel `TypeSupported`/`IsUnsigned`/`AlignedType`/`loadColumnTypedFromBuf` (+DATE arm), NDB API `NdbAggregator::TypeSupported` (+Date) + `Sum`-over-Date reject, RonSQL `build_cte_virtual_tables`/`resolve_chained_column_type` (Date→Bigunsigned) + `ResultPrinter` (`aggregate_arg_is_date` + unpack). **Phase 5 (GROUP-BY-over-DATE) needed no kernel change** — `initGBTypes` is type-generic (`cmpDate`, no charset), deterministic raw-byte hash. MTR: agg-d17a (re-agg via CTE_LOOKUP), agg-d17b (CTE_SCAN root), agg-d17c (GROUP BY date) green ×5 topologies. index-9 re-tagged D9 (DATE index-scan root, separate). Doc also carries the **temporal-extension scope** (all SHIPPED): YEAR + DATETIME2 + TIME2 + TIMESTAMP2 (big-endian load + my_time decode; 6-bit kOpLoadCol type field + `ndbd_support_agg_wide_type` gate for the >31 types).  TIMESTAMP2 MIN/MAX is TZ-independent (epoch order is absolute); display is UTC via the lock-free `ronsql_utc_sec_to_TIME` (`get_date_from_daynr`, no glibc tzset_lock — cf. commit 808bb79ce23), with the suite loading + comparing under `time_zone='+00:00'`.  MTR: agg-d17a..j
- `cte_filter_phase_k.md` — Phase K: ANTI_JOIN promotion for WHERE col IS NULL on LEFT JOIN RHS (RonSQL)
- `cte_filter_phase_l.md` — Phase L: JOIN_AGG_COMPLETE robustness — checkCteReady / continueJoinAggRedistribute idempotency, unified requestId correlation in DBTC, single-owner DBLQH routing, phase-report deduplication, test coverage (kernel + DBTC, plan-only)
- `cte_filter_phase_l_commit5.md` — Phase L commit 5: cleanup follow-up — drop redundant `m_redist_mutex` and retire legacy `m_cteCompleteOutstanding` counter
- `cte_filter_phase_m.md` — Phase M (shipped, `a28ed6d817f`): aggregation-program BranchReg control flow removed; pair-op kernel emission rewritten to embedded normal-interpreter (`READ_AGG_REG_TO_REG + BRANCH_*_REG_REG`) + `Mov`; cross-table subquery / DNF filter emission rewritten; `NdbAggregator::BranchReg*` API and `kOpBranchReg*` opcodes removed
- `cte_filter_phase_m1_inventory.txt` — Phase M.1: plain-text BranchReg / Greatest2 / Least2 inventory and guard checklist
- `cte_main_joinagg_suppressed_nextreq_plan.md` — Plan for the main-SELECT aggregate hang: root cause is DBSPJ counting agg-consumed CTE_LOOKUP rows into `Request::m_rows`, defeating the `handleJoinAggNextBatch` self-continue so a mid-fragment `SCAN_FRAGCONF` reaches DBTC and parks the fragment in DELIVERED forever.  Fix is a 2-line `m_aggNodes` guard in `execCTE_LOOKUP_CONF` (mirrors the scan-side rule at DbspjMain.cpp:13173); DBTC additionally *refuses* case-2 involvement — mid-fragment confs → `scanError` (new error 1270 `ZCTE_AGG_BATCH_PROTOCOL_ERROR`), non-close `SCAN_NEXTREQ` ignored, suppression branch asserted DELIVERED-free.  Supersedes the earlier DBTC self-send `SCAN_NEXTREQ` proposal (rejected: `disconnectMaliciousNode`-on-self race, SCAN_TABREF-to-self ndbabort)
- `cte_nextreq_plan.md` — SCAN_NEXTREQ flow control for CTE main-SELECTs: overview + phase index
- `cte_nextreq_phase_1.md` — Phase 1: fix CTE_SCAN_REQ continuation plumbing (scanIterI, SignalLengthContinue)
- `cte_nextreq_phase_2.md` — Phase 2: SCAN_NEXTREQ flow control for CTE_SCAN root
- `cte_nextreq_phase_3.md` — Phase 3: CTE state lifetime audit across SCAN_NEXTREQ pauses
- `cte_nextreq_phase_4.md` — Phase 4: NDB API tests for multi-batch CTE main-SELECTs
- `cte_outer_join_plan.md` — CTE outer-join support: overview + phase index
- `cte_outer_join_phase_1.md` — Phase 1: CTE_LOOKUP outer-join child (API + agg-feed)
- `cte_outer_join_phase_2.md` — Phase 2: scanCte as outer-join LEFT-side parent (verification)
- `cte_outer_join_phase_3.md` — Phase 3: CTE_SCAN as outer-join child
- `cte_outer_join_phase_4.md` — Phase 4: testCteNdbApiOuterJoin.cpp consolidated tests
- `cte_outer_join_phase_5.md` — Phase 5: CTE_LOOKUP agg-feed NULL injection (was next_steps 6a)
- `ronsql_cte_plan.md` — RonSQL client-side wiring for all the above CTE
  features (Phases A–H + Phase P-GB). Phase A + A0 + B.1 landed; Phase B.2
  next. Portable copy of the plan-mode file — use this to resume the
  session on another machine.
- `where_filter_analysis.md` — WHERE filter + pushed aggregation bug analysis (3 bugs, AccessPath tree shapes, condition push pipeline)
- `where_filter_analysis.html` — Visual version with SVG diagrams

### Key Source Files
- `DblqhMain.cpp` — Signal handlers for JOIN_AGG_SETUP/COMPLETE/RELEASE, scan processing, sendScanFragConf
- `DblqhProxy.cpp` — Routes setup/complete/release signals; manages JoinAggregationState pool
- `Dblqh.hpp` — ScanRecord (m_rows_examined, m_join_agg_state_key, m_join_agg_evict_rows)
- `JoinAggregationState.hpp` — Shared state struct for join aggregation across LDM threads

### Related Files in Other Blocks
- `storage/ndb/src/kernel/blocks/dbtup/AggInterpreter.hpp/.cpp` — Aggregation engine (chunk allocator, group-by hash map)
- `storage/ndb/src/kernel/blocks/dbtup/DbtupExecQuery.cpp` — `handleJoinAggRow()` interception point
- `storage/ndb/src/kernel/blocks/dbspj/CLAUDE.md` — DBSPJ protocol documentation
- `storage/ndb/include/kernel/signaldata/JoinAgg.hpp` — Signal definitions
- `storage/ndb/include/kernel/signaldata/ScanFrag.hpp` — ScanFragConf (includes rowsExamined field)

### Signal Flow
```
DBTC → JOIN_AGG_SETUP_REQ → DblqhProxy → creates JoinAggregationState
DBSPJ → SCAN_FRAGREQ (with join agg flag) → LDM threads → handleJoinAggRow → AggInterpreter
DBSPJ → JOIN_AGG_COMPLETE_REQ → DblqhProxy → merge + finalize → TRANSID_AI results
DBSPJ → JOIN_AGG_RELEASE_REQ → DblqhProxy → cleanup
```

### Testing

All tests in `storage/ndb/block_unit_test/`. Build from debug_build, run with
`-c <connect_string> -m <mysql_port> [--verbose]`.

| Target | Signal Path | Description |
|--------|-------------|-------------|
| testJoinAgg | Direct DBLQH | All agg types, GROUP BY, eviction, mutex-free, flow control |
| testJoinAggSpj | DBTC→DBSPJ→DBLQH | Full QueryTree path, empty/single row, large dataset |
| testJoinAggNdbApi | NdbQueryBuilder API | 4 tests: SUM/GROUP BY, COUNT+SUM, multi-agg, 3-way join |
| testOuterJoinAggNdbApi | NdbQueryBuilder API | 2-way outer join: scan-lookup, scan-scan, COUNT(*), multi-batch |
| testMultiOuterJoinAggNdbApi | NdbQueryBuilder API | Chained outer join: 3-way LEFT, 4-way mixed LEFT+INNER |
| testCaseAgg | Direct DBLQH | CASE expression in aggregation |
| benchJoinAgg | Direct DBLQH | Performance: pipelined lookups with linked attrs |
| bench_q12_tpch | Direct DBLQH | TPC-H Q12 with CASE, CHAR comparison, date filters |
| bench_q12_dbtc | DBTC→DBSPJ→DBLQH | TPC-H Q12 through full orchestration |
| bench_q9_dbtc | DBTC→DBSPJ→DBLQH | TPC-H Q9: 6-table join, multi-level linked attrs |
| load_tpch | — | TPC-H data loader for bench_q9_dbtc |
| load_ronsql_large | — | Large-data loader (lg_cust/lg_orders) for the `ronsql_large` MTR suite; reachable from the test as `$NDB_PUSH_AGG_DIR/load_ronsql_large` |

Build all: `make -j$(sysctl -n hw.ncpu) testJoinAgg testJoinAggSpj testJoinAggNdbApi`

### ERROR_INSERT Codes
- **5090** (DblqhProxy.cpp): Forces `setMaxGroups(3)` on AggInterpreters during
  JOIN_AGG_SETUP_REQ, triggering eviction when a 4th distinct group arrives

### DUMP State Commands (debug/test builds only)
- **2359** (LqhSkipTcNodeCheck): Disables the `get_node_status(refToNode(tcRef)) != ZNODE_UP`
  check in LQHKEYREQ processing, allowing LQHKEYCONF to be sent to non-data-node refs
  (e.g., test programs connected as API nodes)
- **2360** (LqhRestoreTcNodeCheck): Re-enables the tc node status check

Both are guarded by `#if defined(VM_TRACE) || defined(ERROR_INSERT)`.

### Key Design Patterns
- **TransientPool::seize() doesn't call constructors** — all JoinAggregationState fields
  must be explicitly initialized after seize in DblqhProxy::execJOIN_AGG_SETUP_REQ
- **Two interpreters**: NDB interpreter (WHERE clause) runs first, then AggInterpreter
  (aggregation) runs via handleJoinAggRow, just before sendReadAttrinfo
- **Version-gated signals**: New fields (e.g. rowsExamined in ScanFragConf) use
  ndbd_support_xxx() checks in ndb_version.h.in for backward compatibility
- **Batch counter save-before-reset**: In sendScanFragConf, save counters to locals
  before the batch reset block, since reset happens before signal population

### NdbInterpretedCode: Inverted Inequality Branches

**CRITICAL**: The NDB interpreter's inequality branch instructions are implemented
**backwards** from their names. EQ and NE work as expected, but LT/LE/GT/GE are
inverted (see `NdbScanFilter.cpp` line 560: "the interpreter cmp-code has been
implemented backwards, such that it branch on non-matches").

| Method name       | Actually branches when |
|-------------------|------------------------|
| `branch_col_eq`   | col == val (correct)   |
| `branch_col_ne`   | col != val (correct)   |
| `branch_col_lt`   | col **>** val          |
| `branch_col_le`   | col **>=** val         |
| `branch_col_gt`   | col **<** val          |
| `branch_col_ge`   | col **<=** val         |

This applies to both attr-vs-constant and attr-vs-attr variants.

**Root cause**: In `DbtupExecQuery.cpp` interpreterNextLab(), the comparison result
`res1 = cmp(col, val)` is tested with inverted conditions:
```cpp
case Interpreter::LT:  res = (res1 > 0);  break;  // branches when col > val
case Interpreter::GE:  res = (res1 <= 0); break;  // branches when col <= val
```

**NdbScanFilter compensates** by double-inverting: it negates the user's condition
(for AND groups) then looks up the already-inverted branch method in table3/table4.

**When building raw interpreter programs** (e.g., for PI_ATTR_INTERPRET in
QN_ScanFragParameters), use the opposite inequality method:
- To reject when `col >= val`: use `branch_col_le` (not `branch_col_ge`)
- To reject when `col < val`: use `branch_col_gt` (not `branch_col_lt`)

### NdbInterpretedCode::getWordsUsed() Includes Meta-Info

`getWordsUsed()` returns `m_instructions_length + numLabels * 2`, but the label
meta-info is stored at the END of the buffer (not after instructions). Reading
`buf[0..getWordsUsed()-1]` includes garbage from the gap between instructions
and meta-info. Subtract `numLabels * CODEMETAINFO_WORDS` (2 words per label)
to get the actual instruction count:
```cpp
Uint32 numLabels = 2;
Uint32 len = code.getWordsUsed() - numLabels * 2;
```

### Debug Trace Macros (DEB_XXX Pattern)

NDB kernel blocks use a consistent pattern for conditional debug logging:

```cpp
// 1. The #define DEBUG_XXX is inside the VM_TRACE/ERROR_INSERT guard
//    to ensure it can never be accidentally enabled in production builds.
#if (defined(VM_TRACE) || defined(ERROR_INSERT))
//#define DEBUG_AGG 1
#endif

// 2. The DEB_XXX macro is always defined (empty in non-debug),
//    so call sites compile in all builds without #ifdef wrappers.
#ifdef DEBUG_AGG
#define DEB_AGG(arglist) do { g_eventLogger->info arglist ; } while (0)
#else
#define DEB_AGG(arglist) do { } while (0)
#endif
```

Usage: `DEB_AGG(("format string %u", value));` — note the **double parentheses**
(outer for macro, inner for function call in the arglist expansion).

To enable: uncomment `#define DEBUG_AGG 1` and rebuild. Only works in debug
builds (VM_TRACE or ERROR_INSERT defined).

**Important**: Do NOT use `g_eventLogger->debug(...)` for debug tracing — it
requires special runtime configuration to produce output. Use the DEB_XXX
pattern instead for developer-activated trace output.

Similar patterns in the codebase:
- `DEB_CONT_SCAN` / `DEBUG_CONT_SCAN` — DblqhMain.cpp, DbtcMain.cpp, DbtupBuffer.cpp
- `DEB_TRANSID_AI` / `DEBUG_TRANSID_AI` — DbtupBuffer.cpp
- `DEB_RATE_QUEUE_DROP` / `DEBUG_RATE_QUEUE_DROP` — DbtcMain.cpp
- `DEB_AGG` / `DEBUG_AGG` — AggInterpreter.cpp
- `PA_INTERP_TRACE` / `DEBUG_PA_INTERP` — AggInterpreter.cpp (partition-filtered variant)

### NDB API Multi-Fragment Workers vs DBSPJ Parallelism

The NDB API's multi-fragment worker model (`m_fragsPerWorker > 1`) bundles multiple
fragments into a single SCAN_FRAGREQ to a single DBSPJ instance. This is controlled
by the `MultiFragFlag` in `SCAN_TABREQ`. The flow:

1. `NdbQueryOperation.cpp`: `m_fragsPerWorker = rootFragments / numberOfDataNodes`
2. If `m_fragsPerWorker > 1`, `MultiFragFlag` is set in `SCAN_TABREQ`
3. In DBTC `sendDihGetNodesLab()`: all fragments on the same node are assigned the
   **same DBSPJ instance** via round-robin (`cspjInstanceRR`), then sorted by
   `primaryBlockRef` so they group together
4. In DBTC `sendScanFragReq()`: fragments with matching `primaryBlockRef` are bundled
   into a **single SCAN_FRAGREQ** with multiple fragment IDs

**Without MultiFragFlag** (the non-multi-frag path): each fragment gets a **different**
round-robin DBSPJ instance, resulting in separate SCAN_FRAGREQs that run on different
TC threads in parallel.

**Impact**: With 1 data node and 4 fragments, MultiFragFlag causes all 4 fragments to
go to 1 DBSPJ instance (1 SCAN_FRAGREQ), while without it, 4 SCAN_FRAGREQs go to 4
different DBSPJ instances across TC threads.

**Aggregate queries disable multi-fragment workers** (`m_fragsPerWorker = 1`) because:
- Aggregation results are per-node, not per-fragment — the API round-trip savings from
  multi-fragment bundling don't apply
- Distributing fragments across DBSPJ instances gives better parallelism across TC threads
- `scanParallelism` in SCAN_TABREQ equals `rootFragments` (one handle per fragment)

Key code locations:
- `NdbQueryOperation.cpp:3044` — `m_hasAggregation` forces `m_fragsPerWorker = 1`
- `DbtcMain.cpp:16861-16914` — multi-frag SPJ instance assignment and sorting
- `DbtcMain.cpp:17241-17243` — non-multi-frag round-robin SPJ instance per fragment
- `DbtcMain.cpp:18525-18604` — MultiFragFlag fragment grouping in `sendScanFragReq`

#### scanParallelism in SCAN_TABREQ

For JoinAgg queries, DBTC reads `scanParallelism` from the SCAN_TABREQ signal
(`DbtcMain.cpp:15692`) and uses it to allocate that many `ScanFragRec` handles
in `initScanrec`. This determines the **maximum concurrent fragments**. After
DIH reports the actual fragment count (`tfragCount`), `scanParallel` is overridden
to `tfragCount` for fragment iteration, but the number of pre-allocated handles
remains fixed. Excess handles (if `scanParallelism > tfragCount`) are set to
"empty result" in `sendFragScansLab`.

For aggregate queries, `scanParallelism = rootFragments` (since `m_fragsPerWorker = 1`
and `m_workerCount = rootFragments`), which matches `tfragCount` exactly.
