MGM_CONN_STRING=
NDB_MGMD_NODE_ID=

# TODO: Add this to the original cloud setup
# Setting this to *anything* will cause an initial start
INITIAL_START=

# TODO: Add this to the original cloud setup
# This will cause all files generated by RonDB to have this prefix
#   (this includes the PID file).
# The default for this is ndb_<node_id>.
SERVICE_NAME=
